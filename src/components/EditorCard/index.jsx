import styles from './EditorCard.module.css';

const EditorCard = () => {
    return (
        <div className={styles.editorCard}>
            Editor Card
        </div>
    )
}

export default EditorCard
